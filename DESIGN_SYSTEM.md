# football-sim デザインシステム（ui-designer 必読）

**目標品質＝"作り込まれたモバイルゲームUI"**（個人制作っぽさを脱し、商用ゲームとして通用する完成度）。
着手前にこの1枚を読み、**トークンとスケールに載せて**設計する。抽象語で判断しない＝ここに定義された値から選ぶ。

トークンの正本は `css/league-ui.css` の `:root`（`--lg-*`）。**色・余白・角丸・影・動きは必ず `var(--lg-*)` を使い、生値をハードコードしない**（`#fff`/`12px`/`rgba(...)` 直書き禁止。新しい役割が要るなら :root にトークンを足してから使う）。

---

## 1. カラー（役割で選ぶ・見た目で選ばない）
- 面: `--lg-bg-0/1/2`（背景）→ `--lg-surface / -2 / -3`（浮いたカードほど明るく＝elevation）。境界は `--lg-line`（強）/`--lg-line-soft`（弱）。
- 文字: `--lg-text`（主）→ `--lg-text-dim`（副）→ `--lg-text-mute`（ラベル）→ `--lg-text-faint`（最弱）。**背景を変えたら必ず文字色も指定**。暗背景＋暗文字/白背景＋白文字は不合格。
- 意味色: 勝`--lg-win`/分`--lg-draw`/負`--lg-loss`、良`--lg-good`/悪`--lg-bad`、情報`--lg-info`。
- プレミアム: `--lg-gold`/`--lg-gold-2`（優勝・協会・表彰など"格"を出す所だけ）。金は**要所のみ**＝多用すると安っぽい。
- コントラスト: 本文はAA（4.5:1）以上を狙う。`--lg-text-mute` は10px以下のラベル専用（本文に使わない）。

## 2. タイプスケール（この段から選ぶ・中間値を作らない）
| 役割 | px | weight | 用途 |
|---|---|---|---|
| caption | 10 | 800 | overline/ラベル・letter-spacing 2–3px・大文字 |
| meta | 12 | 600–700 | 補助情報・チップ内テキスト |
| body | 13.5 | 400 | 本文・総評。line-height 1.8–1.9 |
| label | 15 | 800 | セクション見出し内の強調 |
| subhead | 18 | 800 | サブ見出し |
| title | 22 | 900 | 画面タイトル・マストヘッド |
| hero | 30 | 900 | 特大の決定的瞬間（優勝など） |
| display | 46 | — | エンブレム/絵文字級 |
- **原則**: 1画面で使うサイズは3–4段まで。10/11/12/13を混在させない（＝散らかる。今のCSSはここが弱点）。
- 日本語は字面が詰まるので line-height は英より広め（本文1.8+、見出し1.25+）。

## 3. 余白スケール（8pxベース・一定リズム）
`4 / 8 / 12 / 16 / 24 / 32`（px）から選ぶ。既存トークン `--lg-pad-s(8)/-m(12)/-l(16)`。
- カード内 padding: 14–16。セクション間 gap: 16（読み物）/ 12（密）。要素間 gap: 8。極小: 4。
- **禁止**: 7px/9px/13px 等の"なんとなく"余白の乱立、意味のない大きな暗い空き（デッドスペース）、上下の間延び。各余白は「役割があってその大きさ」。

## 4. 角丸・奥行き（elevation）
- 角丸: `--lg-r-s(8)` 小要素 / `--lg-r-m(12)` カード / `--lg-r-l(16)` ヒーロー・大パネル。
- 影: `--lg-shadow` 通常カード / `--lg-shadow-l` 主役・モーダル。影は**奥行きの表現**であって装飾ではない＝浮いてる物にだけ。
- elevation順: 背景 < surface < surface-2 < surface-3。重ねるほど1段明るく＋境界を薄く。

## 5. コンポーネント作法（既存の型に合わせる）
- **カード**: `background:var(--lg-surface); border:1px solid var(--lg-line); border-radius:var(--lg-r-m); padding:14–16px`。
- **セクション見出し**: overline(caption) ＋ 罫線（`.lg-fin-h` 型＝ラベル＋`flex:1`の`--lg-line`罫）。見出しは静かに、中身を主役に。
- **チップ/バッジ**: 角丸ピル、`--lg-surface`＋`--lg-line`。数値バッジは `--lg-gold` 背景＋`--lg-ink` 文字。
- **ボタン**: タップ領域 **最低44px高**。主ボタン＝塗り、副＝`.sec`（枠線）。1画面の主ボタンは1つ。
- **リスト行/テーブル**: ゼブラは弱く、自分/主役の行だけハイライト（`--lg-surface-2`＋左アクセント）。列は右揃え数値。
- **主役（結果カット等）は構図の重心**に。周辺は静かに（密度を落とす）。

## 6. レイアウト / ゾーン（BACKLOG「UI/レイアウト作法」準拠）
- **固定px禁止**＝相対単位・`clamp()`・flex/grid/minmax。`height:822px` の決め打ちをしない。
- 横長3ゾーンの分離構造を維持し、中央をシネマチック試合、左右を判断材料・世界の反応に使う。旧 `zone-top / -center / -bottom` 名は実装上残っていても、縦長へ戻す根拠にしない。
- **長文の読み物**（最終話など）は**専用の全幅・内部スクロール**にする。ハブの固定2カラム枠に読み物量を詰めない（flex潰れ・はみ出しの原因）。読みカラムは `max-width` で1行長を抑える（≈640px）。
- 狭幅（〜380px）とワイドの両方で崩れないか確認（`preview_resize`）。

## 7. モーション
- `--lg-t-fast(.14)` 反応 / `--lg-t-mid(.32)` 遷移 / `--lg-t-slow(.6)` 入場。ease は `--lg-ease`。
- 入場は"下から+フェード"程度に抑制。派手な弾み/点滅は要所のみ。動きは意味に従う。

## 8. テイスト（"上質"の定義）
- **抑制＞装飾**。奥行きは subtle な段差・縁・内側シャドウで出す。強グラデ/ネオン/雑な影は禁止。
- **金は格の演出だけ**（優勝・協会）。日常UIはニュートラルに。
- **リズムが命**＝同じ余白ステップ・同じ角丸・同じ影を反復する事で"設計された"印象になる。
- **内部お手本**: 週刊ベストイレブンカード（黄×黒マストヘッド）・協会ベストイレブン（紺×金）＝この密度と格を全画面の基準に。

## 9. 完了ゲート（自己採点ルーブリック・**全軸2以上で合格**）
build→preview でスクショし、各軸 **0=不可 / 1=甘い / 2=合格 / 3=秀逸** で採点。**1つでも1以下なら直して再スクショ**。
1. **デッドスペース**: 意味のない大きな空き・浮いた要素が無い
2. **余白リズム**: §3 のステップで一貫。中間値の乱立が無い
3. **視覚階層/整列**: 見えないグリッドに整列・主役が重心
4. **タイポ規律**: §2 スケールから3–4段のみ・役割が明確
5. **上質さ**: elevation/縁/アクセントで作り込み・安っぽさ/個人制作臭が無い
完了報告に **before→after の各軸スコア**を必ず添える。甘い所は正直に書く。
