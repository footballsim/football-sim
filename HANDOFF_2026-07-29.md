# 引き継ぎ — 2026-07-29 / 試合パートのグラフィック方針

次セッションの主題: **デイヴ・ザ・ダイバー水準のグラフィックにどう到達するか**の協議。
ユーザー指示: エージェントとCodexを使って複数案を出し、比較して進めてよい。

---

## 1. 次セッションの論点（ここから始める）

参考: **デイヴ・ザ・ダイバー**（Steam app 1868140・MINTROCKET・2023）。
ユーザー指定はメイン動画 0:50〜1:00。分析用フレームと動画は
`/private/tmp/claude-501/-Users-iwasakimitsuru/1fea25c4-.../scratchpad/dave1〜3.mp4 / dave*_sheet.png`
（消えていれば Steam のストアページから `extras/*.mp4` を再取得できる）。

**実物から読み取った要点:**
- 上段＝**斜め分割で顔アップを並べる montage**。マンガのコマ割り文法をドット絵でやっている。
  うちの「対決割り」`_renderShotDuelScene` と同じ構造。
- 下段＝**寿司バーの引き画**。手前カウンター／歩く客／棚の酒瓶／窓外の海と山／提灯と桜で
  6〜8層の奥行き。
- ★ **ショットの種類ごとに絵の縮尺と情報量を切り替えている。**
  引き画＝キャラは小さく単純・環境に情報量を全振り。顔アップ＝キャラを画面いっぱいの精密な絵。
  **1種類の素材で両方を賄っていない。**

**仮説（未検証・ここを協議したい）:**
うちは「マンガスプライト」という中間スケールの素材1種類で、引きも寄りも顔も全部やろうとしている。
2026-07-28〜29 に 3D化・モーキャップのスプライト化で連続して詰まったのも、根はここではないか。
提案する設計は4層に分けること:
1. 引き画（試合の俯瞰）2. 中景（現在のマンガスプライト）3. 顔アップ（カットイン）4. UI画面（監督室）

---

## 2. 今日つくったもの（全てラボ限定・**本番未反映**）

### 演出まわり（js/cutscene.js）
- **`_csFx`** = 静止画に重ねるFX層。`rays`(光芒)/`ring`(衝撃波)/`grade`(彩度burst・drain)/
  `vignette`/`flashes`(観客)/`punch`(寄り+シェイク)/`typeText`(打字字幕)。
  - ★ **ユーザー判断で光芒・リング・観客フラッシュはゴール/失点から撤去**。
    残したのは彩度グレードと失点の赤ビネットのみ＝「絵に物を足す」FXは不採用。
  - `typeText` はAPIのみで未配線（情報を増やす方向のため保留）。
- **`_csCam`** = CAM-01。2.5Dカットアウト＋カメラ。レイヤー深度でパララックス／
  遅れて追うフォロー（lag 0.62・追い切らない）／インパクトの一瞬だけ揺れ／`puppet`で前傾と潰し。
  - 配線済み: ドリブル突破（2経路）・シュート・飛び出し。
  - **ワンツーは実装したがユーザー判断で撤去**（「微妙」）。静止カメラに戻してある。
  - ★ **寄りは片道。戻さない**（ユーザー指示）。戻すと勢いが打ち消される。

### 3D検証（結論: 選手には不採用）
- `_3d_lab.html` … three.js の選手。**画風が割れ、モーションも不自然で不採用**。
  膝の符号バグ（この骨格では屈曲＝正）を修正し、可動域リミットを追加済み。
- `js/bg3d.js` + `_bg3d_lab.html` … **背景だけ**ローポリ3D（潜水艦ゲーム技法＝16色/ディザ/
  ライティング無し）。`_scene_lab.html` の「背景の種類」で切替可（既定OFF）。
  - 現状**1枚絵に絵の完成度で負けている**。勝ち筋は「1枚絵では作れない画」（ポスト際など）。

### モーキャップ（js化せずツール群）
- `mocap/asf2json.py` … CMU の ASF/AMC → 関節ワールド座標JSON。
- `mocap/retarget.py` … → pose-figure/sprite-studio の関節角（**検証済み: 平均誤差0.01〜0.05°**）。
  ヨーを除去して常に正面向き（sprite-studio はカメラ固定運用のため）。
- 出力: `~/sprite-studio/poses/mocap_run/`（走り6）・`mocap_kick/`（キック6）。
- `_mocap_lab.html` … CMUデータの再生台。

---

## 3. 未解決 / 次にやるなら

- **モーキャップ6コマは接地高さが最大9.4cmばらつく**。既存 `make_run_cycle.py` は
  「滞空コマを作らない・6コマとも足元y同一」が要件（そうしないとそのコマだけ体が沈む）。
  → 解は `rootPos` でコマごとに腰の高さを補正すること。**未実装**。
  → そのため**Codexでの実際のスプライト生成はまだ回していない**。
- 3D背景を使うなら「3Dでしか撮れない画」を演出として先に設計する（でないと粗い1枚絵になるだけ）。
- CAM-01 の残り配線先: ロングパス等。

## 4. ラボ入口（常設プレビュー・launchd常駐）
- 演出テストラボ http://localhost:5175/_scene_lab.html
- 3Dプレイヤー実験室 http://localhost:5175/_3d_lab.html
- 3D背景ビューア http://localhost:5175/_bg3d_lab.html
- モーション実験室 http://localhost:5175/_mocap_lab.html

## 5. 検証状況
`node --check` OK / 回帰ハーネス **回帰なし** / 各ラボ 61〜62fps / ページエラーなし。
**ビルド・デプロイはしていない**（football-sim.com も kantoku-lab も今日の変更は未反映）。
