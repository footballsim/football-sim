# DECISIONS — 設計判断ログ

採用した判断と理由を時系列で残す。コンテキストが切れても引き継げるチームの記憶。
新しい判断は上に追記する。

---

## 2026-08-25 — ショートパスをクロス採用時の局所反復方式へ戻す

- **判断訂正**: ショートパスの関節誤差だけを重視して未実証の有料ハード姿勢経路を優先したのは誤り。ユーザー指示により、クロスで採用へ到達した「1コマ生成→合格領域を凍結→問題部位だけを1つずつ修正→比較」の実績経路へ戻した。
- **F3反復**: built-in ImageGenのF3初稿から、独立QAの最優先順に①蹴り脚チェーン、②右肘角、③上体と骨盤の軸だけを個別修正した。v4は方向、脚左右、両腕、肩/胴、5.6頭身、刈上げ、画風、全身クロップを独立QAがPASS。生成背景の近白は人物を再生成せず外周連結領域だけ純白化した。
- **F3採用**: 提示4枚では左から3番目が最良とユーザーが選択。足元へ向けた白目内部23px修正版v3は独立QAを通ったが、ユーザーが「白目が多く違和感」と却下。続けて添付された1474×1682画像をF3として明示採用した。添付と正典 `img/cutscenes/manga_shortpass6/frame_03.png` はSHA-256 `df54ea8579474747ca0357521868e0a38aa951fefa337f437ff435177d7d6bfa`で完全一致し、独立QAもPASS。
- **ゲート**: F3だけが`adopted`。次はF1を1コマだけ同じ局所反復で作り、独立QAとユーザー採用後にF2へ進む。F1/F2/F4/F5/F6が全採用されるまで本編統合、build、push、deployは禁止する。

---

## 2026-08-25 — ショートパスF3の既存Replicate経路を停止

- **実行条件**: ユーザーが累計0.10米ドル以内を明示承認。公式料金`$0.000975/秒`に対し、初回はF3 OpenPose＋専用LoRA・4出力・35stepを`Cancel-After: 80s`、再試行は1出力・30stepを`Cancel-After: 20s`に固定した。理論上の累計料金上限は`$0.0975`。
- **結果**: 2回ともReplicate側で`aborted`となり、生成画像は0枚。再試行を含め品質評価へ進める候補は存在しない。追加課金は停止した。
- **決定**: `fofr/sdxl-multi-controlnet-lora`の当該公開版をショートパス制作経路から外し、同条件の再課金を禁止する。F3が未校正のため残り5コマは生成しない。次は、実行時間を事前検証できる十分なVRAMのGPU環境、または料金上限を外部から確実に制御できる別の意味付きSDXL OpenPose実行基盤が必要。

---

## 2026-08-25 — ショートパスSDXLのローカル8GB経路を不採用

- **実機結果**: 承認COCO-18、SDXL base、専用SDXL LoRA、Xinsir OpenPose SDXLをM1 8GBのComfyUIでF3へ適用した。通常OpenPoseはMPS 9.08GBでOOMし、384×512の1ステップも10分超で未完了だった。
- **軽量化の切り分け**: Stability AI公式Canny Control-LoRA rank128（SHA-256 `56389dbb...`）とByteDance公式SDXL-Lightning 2-step（SHA-256 `04fafc77...`）へ置換すると完走した。しかし256×320の骨格入力はCannyに人体関節でなく色付き物体輪郭として読まれ、人物不成立。拘束なし診断では人物になったため高速LoRA自体は動作したが、上半身クロップ、画風、白背景、5.6頭身が不合格だった。
- **決定**: ローカルM1 8GBでのSDXL再試行を禁止し、全出力を内部失格としてユーザー候補へ出さない。次は十分なVRAMのGPUで、専用SDXL LoRA＋意味付きSDXL OpenPoseをネイティブ寄り解像度で実行する。有料Replicate経路は料金発生前の明示承認を必須とする。F3が全ゲートを通るまで残り5コマへ進まない。

---

## 2026-08-24 — 画像再開時の外部脳接続とショートパス体型ゲートを必須化

- **再発原因**: 承認済みの脚・腕・画風・刈上げはショートパス台帳へ固定されていたが、`PLAYER_BODY_CANON.md`の5.6頭身数値契約が専用生成ゲートの入力集合に入っていなかった。またObsidian外部脳は存在していても、画像再開時の起動手順では任意参照に留まり、2026-08-13以降のショートパス経緯が未蒸留だった。
- **引き継ぎ**: 画像の再開・再生成・品質修正では、repo正本を読んだ後に`~/Documents/2nd-Brain/football-sim.md`の対象シーンを必ず検索する。採否・数値・禁止入力はrepoを優先し、Obsidianは経緯と失敗理由を補う。古い外部脳を黙って最新扱いしない。
- **生成ゲート**: ショートパスは承認トレース、座標リグ、姿勢中立スタイルボード、右向き刈上げ頭部に、体型計測画像を加えた5入力を必須化する。F3を先行校正し、5.40–5.80H、頭幅0.97–1.07H、肩幅1.32–1.48H、頭高差3%以内を通過するまで残りを生成しない。1枚でも失格なら6コマセットをユーザーへ提示しない。
- **ハード姿勢拘束の切り分け**: ローカルComfyUIのSD1.5 OpenPose ControlNetで、承認台帳から決定論的に変換したCOCO-18骨格へF3全身を追従させることには成功した。ただし手元のPixel checkpoint＋IP-Adapterは承認画風を維持できず全校正出力を内部却下。既存の専用LoRAはSDXL用でSD1.5非互換と実測したため、残り5コマへ進まない。次はローカルSDXL本体＋SDXL OpenPose ControlNetの導入、または従量課金Replicate経路の明示承認が必要。

---

## 2026-08-23 — コンテキストパックはセクション優先で抽出

- **問題**: ファイル先頭からの単純切り出しでは、後半にある検証ゲートや未完了タスクが予算到達前に欠ける。
- **決定**: `context:pack`はガードレール、検証ゲート、現在地、意思決定、AUTO項目などの見出しを優先し、テーマ固有ファイルを追加する。文字数上限は維持する。
- **効果**: 同じ文字数でも次回判断に必要な情報の密度を上げ、長大な文書の再読込を減らす。

---

## 2026-08-23 — 週次レポートの判定軸を固定

- **出力**: 直近7日間のコミット、期限超過タスク、次週最大3タスク、検証入口を固定項目にする。
- **Go/No-go**: tracked差分が無く、期限超過タスクが無い場合だけ機械的にGO。それ以外はNO-GOとして理由を提示し、ユーザー専用タスクを自動実行しない。
- **境界**: レポートは読み取り専用。コード変更、git操作、デプロイ、外部送信は自動化プロンプト側でも禁止する。

---

## 2026-08-23 — 外部脳とトークン削減の入口を標準化

- **外部脳の正本**: リポジトリの意思決定・バックログを基礎に、`npm run brain:snapshot` がブランチ、HEAD、未完了タスク、最近の判断、検証入口だけを短く出力する。Obsidian等へ貼り付ける際の要約形式を固定し、会話履歴への依存を減らす。
- **トークン節約**: `npm run context:pack -- --focus=<missions|release|ui> --budget=18000` を次回セッションの最小読込入口とする。大きな文書は文字数上限で切り、テーマ外のファイルを再読しない。
- **安全境界**: どちらも読み取り専用の出力で、セーブ、試合ロジック、デプロイ、外部送信は変更しない。週次の定期実行は次段の自動化タスクで扱う。

---

## 2026-08-23 — 監督ミッションMVP（デイリー／ウィークリー／累計）を設計

- **デイリー**: 1日1試合の制約を前提に、勝点1・1得点・完封のいずれか、または試合前準備1回の「1試合で完結する」目標を日替わりで1件提示する。複数試合や連続勝利を要求しない。
- **ウィークリー**: 3試合指揮・勝点5・5得点・2完封・異なる3試合日の準備のいずれかを週1件提示する。同じ試合日の準備3コマは1日分として扱う。
- **累計**: 指揮試合数・勝利数・得点・完封をキャリア記録として蓄積する。
- **実装境界**: `manager.missions` は任意フィールドとして遅延生成し、セーブ版数・試合結果・乱数・デュエルロジックは変更しない。lab限定の`missions.js`として既存のシーズンハブから開く。
- **未完了ゲート**: `manager.missions`という保存フィールド追加はsave schema例外に該当するため、ユーザーの保存仕様承認後に`game-main`へ統合する。Steam公開・権利・価格等のゲートは別途維持する。

---

## 2026-08-22 — クロス演出を採用し、未承認シュート4コマを本編から撤去

- **クロス採用**: ユーザー目視を経て、左足クロス6コマを採用した。選手とボールは左から右へ同じ78pxを前進し、接触後だけボールを加速する。人物はheading／overheadに近い表示面積、ボール半径は10pxとし、終端660msでcanvas右端から自然に完全退出する。縮小カメラ効果は不採用とし、全時間native scaleを維持する。
- **シュート復元**: ユーザーが採用していない`manga_shot_adopted`／`manga_shot_cinematic`を通常試合経路から外し、`67263a2^`時点の2拍シュートと決定論的な対決割り／GK顔経路を完全復元した。不採用画像は削除せず、Scene Labの比較項目に隔離する。
- **品質・反映**: `5e6b3a4`〜`dba345f`。4解像度、console 0、headless 21本、回帰1500、独立QA／ReviewerがPASS。2026-08-22にユーザー採用後、`game-main`へ統合・pushし、Basic認証中のkantoku-labへProduction SHA `dba345f`で反映した。no-cacheでroot／`_scene_lab.html`／`js/cutscene.js`の401・`no-store`を確認した。

---

## 2026-08-20 — GFX-06: 左足クロス6コマをScene Lab正典として採用

- **ユーザー採用**: 実写動画 `0LvI27BgJO8` の5:50–5:52を動作参照にし、助走→踏み込み→振り→接触→フォロースルーを6コマへ分解した。最終コマは左足をscreen-rightへ振り抜き、右足をdown-leftの軸足、奥の左腕を約90度曲げて拳を腰側へ置くポーズで確定した。
- **体型・画風**: 2026-08-19の5.6頭身カノンを維持。1〜5コマは承認済み`cross_six_final_candidate_v7.png`、6コマ目は承認済み`exec-52b61212-ce40-41ed-8b16-7b0fd740b256.png`を正本とし、透過処理以外で頭身・顔・身体・ポーズを変えない。
- **実装境界**: `0ba6f20`＋`0dc1035`で、6枚を`manga_cross6`へ分離し、Scene Lab専用の一回再生として追加した。各コマの腰アンカーで位置を固定し、5コマ目の右端スパイク付近でコード側ボールが接触、6コマ目でscreen-rightへ離れる。承認方向を確認しやすいようLabではnative screen-right固定。本編のcross／longpass routingは変更していない。
- **cold-load契約**: 6画像すべてのdecode完了後にのみ再生時計を開始する。破損・5秒タイムアウトは明示エラーで有限停止し、DOMから外れたcanvasも停止する。遅延到着後にf1→f6を順番どおり描く動的テストを追加した。
- **品質証拠**: 6PNGはRGBA、高さ336px、全辺8px透明余白、黄key 0。3キット再配色、ボール同期、4解像度の横切れなし、console 0、対象HTTP 200を確認。独立QA／Reviewer、`release:source`全20本、回帰1500、diff checkがPASS。
- **次の境界**: 今回は動作・画像の正典化とScene Lab比較まで。本編クロスへの採用、攻守結果ビート、左右反転は別タスクとして再度ユーザー目視ゲートを通す。権利・AI申告の最終確定はIP-01に残す。

## 2026-08-19 — 試合選手の体型を5.6頭身カノンへ固定

- **ユーザー指定**: 生成済み2画像の体型・等身を理想形として採用する。特に体に対する頭の大きさを今後の全試合画像で固定し、ポーズごとの頭身ブレを許容しない。
- **実測**: 1254px角の両マスターで、髪を含む頭頂〜顎下は183pxで一致。頭幅は約1.02H、肩幅は約1.40H、復元身長の中心値は5.60Hとした。許容範囲は5.40–5.80H、同一シーケンスの頭高差は3%以内。
- **制作契約**: 体型参照、実写ポーズ、画風参照を別入力として扱う。ダイナミックさを出すために頭だけを小さくせず、ズーム時は人物全体を一様拡縮する。機械計測と別系目視の両方をユーザー採用前に通す。
- **正本**: 詳細な定義、生成プロンプト固定句、受入ゲートは [design/PLAYER_BODY_CANON.md](design/PLAYER_BODY_CANON.md)。参照ポーズや黒タイツ等の衣装は体型カノンに含めない。

## 2026-08-18 — GFX-05ロングパス案を不採用、実写Shorts参照へ戻す

- **ユーザー判定**: `codex/gfx-05-longpass-cinematic`の攻撃4拍＋守備3状態は、内部QA/Reviewを通過したが画そのもののクオリティが不足するため不採用。`game-main`へ統合せず、Labへも反映しない。
- **制作手順の修正**: 次の候補は画像生成から始めず、YouTube Shortsで全身・助走・踏み込み・インパクト・振り抜きが追える実写を先に選ぶ。実写のキー姿勢を固定してから、保護済みheading/overhead/shotの画風へ変換する。
- **既存資産**: `~/sprite-studio/out/ref_cross5/`に、以前のクロス制作で使った実写キー姿勢、6コマ比較、生成途中画像が残っている。`ALL6_FINAL_REAL.png`は実写6局面と対応ドット絵、`cross6_grid_result.png`は生成6コマの確認用。ただし手の変形・顔ブレの既知問題があるため、そのまま製品採用はしない。
- **参考候補**: YouTube Shorts `ILBqcx-vDPk`（長距離クロスの接触点・足首・助走速度）と`3n0jZZZorAc`（腰の向き・足の内側・フォロースルー）を一次候補とする。映像は動作参照だけに使い、フレームや人物意匠を製品へ複製しない。

## 2026-08-17 — 試合パート画像はシーンごとにユーザー目視採用を必須化

- **ユーザー決定**: 試合パートのグラフィックは、次のシーンへ進む前にユーザー確認を入れる。包括委任の例外として、1シーンごとに採否を明示的に確定する。
- **固定フロー**: 生成→仮配線→機械検査→独立QA/Review→最終候補画像と実機プレビュー提示→ユーザー採用→`game-main`統合/build/push/deploy→次シーン。採用前はブランチ内に留める。
- **現在地**: GFX-05ロングパスは内部QAの修正中。修正版が緑になっても、ユーザー採用までは統合・Lab反映・次のドリブル生成を行わない。

## 2026-08-17 — GFX-04: 保護シュートを残し、追加4拍を決定論ローテーション

- **ユーザー決定**: `manga_shot_adopted`の既存シュート4枚も、ヘディング／オーバーヘッドと同じ保護対象にする。ただしシュート演出は複数あってよいため、既存版を置換せず追加バリエーションとして制作する。
- **実装**: `752fd6b`で、構え→踏込→接触→振抜きのシネマチック4拍を追加。攻撃側キット／肌へ実行時再配色し、選手名＋対戦相手＋試合時刻の既存ハッシュで保護版と交互表示する。`MangaRecolor`が無い環境は必ず保護版へ戻し、試合RNG・結果・デュエル・セーブには触れない。
- **生成来歴**: 2026-08-17、built-in ImageGenで生成（生成ID `exec-3c9e6be9-c067-4154-8c33-555c663626b5.png`）。保護済みheading／overheadを画風参照にし、架空選手、無地の分離色キット、4ポーズ、文字・ロゴ・ボールなしを指定。生成元の市松背景は背景連結領域だけを除去し、透明列で4人を分離、最近傍で高さ320pxへ整えた。生成元はrepoへ入れず、最終RGBA4枚だけを採用する。
- **QAで修正した点**: 初版は4枚目へ隣ポーズの靴が混入し、端余白とクロマ縁も不足していた。再分割後は全辺6〜7pxの透明余白、40px以上の独立alpha成分なしを自動検査。Brazil／Netherlands／Franceの3配色、新旧両経路、4解像度、console 0、対象画像HTTP 200、headless 19本を確認した。
- **権利境界**: 実在選手・クラブ意匠・ロゴ・文字・透かし・国旗・明白な著名人類似は独立目視で認めず。Steam向けの採否とAI申告の最終確定はIP-01の人間ゲートに残す。

## 2026-08-17 — FN-02を残したまま、ストア向けグラフィック整備を先行

- **優先順（ユーザー決定）**: 架空名の全表示面監査FN-02は削除せず後ろへ送り、グラフィックを先行する。Store Candidate凍結前にFN-02へ戻る。背景3点の後は記者会見背景より試合シーンを優先し、保護対象以外を作り直す。
- **GFX-01**: `68b062e`で1280×400のボード面談背景を追加。配置済み画像だけをpreloadし、読み込み後にcanvasを再描画する。未配置11スロットへの404を初回QAで検出し、boardroom限定preloadへ修正後にPASS。
- **GFX-02**: `5b0f346`で1280×720の無人監督室を追加し、`457d215`で通常ハブの背景が既存CSSに隠れるP1を修正した。通常ハブでは表示し、節進行／シーズン最終話の全面`.lg-se`では隠す。1920×1080、844×390、800×360、667×375で独立QAし、consoleエラー0、不要画像404なし、headless 18本PASS。
- **GFX-03**: `e953603`で1280×720の夜のスタジアム背景を追加。低いピッチ視点、左右のスタンドと投光器、中央の暗い負領域を採用し、VS／KICK OFFの両ビートで再利用する。4解像度で全画面描画・主要UI切れなし・consoleエラー0、配置済み3画像以外の要求なし、headless 18本PASS。
- **画作り**: 監督本人の外見・服装成長と衝突しないよう背景に固定人物を置かない。濃紺の夜景、抑えた暖色灯、中央のUI安全領域を共通語彙とし、漫画枠ではなく成熟したシネマチックな監督ドラマへ寄せる。
- **権利境界**: built-in ImageGenで生成し、文字、透かし、実在ロゴ、国旗、著名人類似を独立目視した。採否とSteamのAI申告の最終確定はIP-01の人間ゲートに残す。

## 2026-08-17 — FN-01: 内部データを変えず、8つの架空クラブ世界を採用

- **実装**: 現行リーグの8内部チームIDに対し、日英のオリジナルクラブ名、固有色、国旗を使わない抽象crestを割り当てた。選手表示名は各クラブの地域性に対応する8言語圏の名前プールから決定論的に生成し、日英のフルネーム重複を避ける。
- **互換性**: `long_name`、能力値、`default_system`、セーブの内部キーは変更しない。実名表示へ戻した時は重複内部IDを含む全63チームの元表示・色・flagを完全復元し、再び架空表示へ切り替えても同じ名前になる。
- **QAで修正した点**: 旧ベルギーデータと現行リーグで共有する5選手が汎用名へ流れる問題を、現行リーグ地域の優先割当で解消した。架空名に実名の短い文字列が含まれる4件と、部分登録後にクラブ色・crestが実データへ戻る問題も修正した。
- **品質証拠**: `010ea5b`を実装後、QA/Review指摘を`8de6652`で修正。専用76/76、FN-00 17/17、`npm run release:source`の全headless 16本、独立QA、独立ReviewがPASS。固定コミットからbuildし、Basic認証中の`kantoku-lab`へ反映した。
- **権利境界**: 限定的な完全一致確認では明白な同名クラブを認めなかったが、法的クリアランスの確定ではない。名称・crest・配色を含む最終採否はIP-01の人間ゲートで行う。

## 2026-08-16 — クラブスタッフ拡張を調査し、収益化をサブスクへ一本化

- **収益化（ユーザー決定）**: 解任回避はサブスクだけの特典とし、従量課金、消費アイテム、都度購入を廃止する。前日の「課金アイテムまたはサブスク」は本決定で上書き。通常状態でも定性的な助言は得られ、サブスクでは上位分析体制の詳細データを閲覧できる。解任回避の既定案は1シーズン1回の理事会再交渉。能力値、RNG、試合結果を直接購入しない。
- **Steam境界**: Steam公式は定期購読を可能としているが十分にサポートされていないと明記し、開発者側サービスによる契約状態管理も必要。12/10のオフラインRelease Mustには入れず、発売後に価格・契約・バックエンド・公平性を人間ゲートで確定する。
- **前回提案の採用（ユーザー決定）**: 監督の名前＋数種類の外見プリセットと、サポーター代表・担当ライター・スカウト担当・スポンサー担当の最小群像を12月版へ入れる。服装成長は発売後。表現基準は12歳以上相当、怪我は流血なし、SNSは厳しい批判まで可だが差別・脅迫・人格否定は禁止。
- **スタッフ調査**: 実在クラブの公式情報から、コーチング、分析、医療、フィジカル、栄養、心理、選手ケア、編成、育成、運営、広報、サポーター対応、商業の部門を確認した。全員を個別管理せず、各部署を1人の名前・外見・口調を持つキャラクターで代表させる。
- **導入原則**: スタッフは正解を自動実行せず、情報、異論、リスク、選択肢だけを渡す。発売時は既存画面へ登場させ、専用スタッフ管理画面や新しい深い効果は作らない。追加役職の採用判断と発売後の段階案は [CLUB_STAFF_PLAN.md](CLUB_STAFF_PLAN.md)。

## 2026-08-15 — 「漫画」からシネマチックな監督ドラマへコンセプトを補正

- **試合表現（ユーザー決定）**: 「1試合が漫画の一話」「サッカー漫画と中継画面のハイブリッド」を製品コンセプトから外す。大事なのは、ファミコン版『キャプテン翼』のように選手へ寄り、プレーの一瞬を大きく切り取る**ダイナミックでシネマチックな試合演出**。既存の `manga_*` ファイル名や過去タスク名は履歴・内部名として残り得るが、今後の企画、UI、ストア訴求の判断語にはしない。
- **ドラマの中心**: 主人公は監督。推し選手はおまけ的要素で発売時の核にしない。宿敵監督も時々登場する演出でよく、深い因縁システムへ拡張しない。
- **作品トーン**: 『GIANT KILLING』を参考に、解任を含む厳しいリアルな監督ドラマと、時々コミカルな人物描写を両立する。作品固有の物語・人物は模倣せず、サポーター、スカウト担当、ライター、スポンサーまでクラブを取り巻く登場人物として扱う群像性を参照する。
- **ターゲット**: 戦術/監督ゲーム層、サッカードラマ層、操作不要を好むライト層の3層を対象にする。訴求の第一顧客は「サッカーの戦術と人間ドラマに興味はあるが、Football Managerほど重い管理は求めない層」とし、機能を3方向へ分裂させない。
- **監督アイデンティティ**: 名前と外見を設定できる。開始時は質素な服装で、キャリア進行により上質な服装を選べる方向を採用。発売時ティアは別途判断する。
- **収益化方向（2026-08-16に上書き）**: 当日は課金アイテムまたはサブスクを検討したが、翌日にサブスクだけへ一本化した。能力値・RNG・試合結果を直接購入しない原則は継続。
- **架空世界**: 架空クラブと、実在を連想できる地域性を組み合わせる。実在クラブの名称・紋章・配色・キットを再現せず、地域の空気をオリジナル設定へ落とす。
- **未決のまま残す項目（2026-08-16に一部解消）**: 追加スタッフを12/10版へどこまで入れるか。表現、監督初期設定、最小4人、サブスク一本化は翌日に決定済み。

## 2026-08-14 — 通常開発をCodexへ包括委任

- **ユーザー決定**: タスクごとの承認負担をなくす。ROADMAP/SCOPE/BACKLOGに含まれる通常開発は、タスク分解、専用worktree、実装、独立QA/Review、`game-main`統合、build、`game-main` push、Basic認証中のkantoku-lab反映までCodexが自律実行し、証拠を事後報告する。
- **例外ゲート**: Steam提出・公開・発売、価格/支払い/契約/秘密情報、権利・AI申告の最終確定、baseline、save schema/内部ID、duel logic、スコープ/日程変更だけは停止してユーザー判断を求める。3回失敗、競合、担当外差分、deploy-guard DANGER、権利不明も自動停止する。
- **機械的な境界**: project-local `.codex/rules/football-sim.rules`は直接git書込をpromptへ戻し、workspace外の固定gateway `~/.codex/bin/football-sim-delegated-git.mjs`だけを許可する。`git add ./-A`、reset/clean、`main` push、`npx`/`npm exec`/wrangler直叩きを禁止する。Rules/gateway自体の変更は例外ゲートとし、Full Accessにはしない。
- **変更前決定の上書き**: 下記「4枠の半自律体制」にある`game-main`統合/build/push/kantoku-lab deployの都度人間ゲートを、本決定の包括委任へ置き換える。Steam・権利・基幹データ等の例外ゲートは継続する。

## 2026-08-14 — AUTO-02: headless統合ゲートを採用

- **実装**: `npm run test:all`で自動検出12本＋manifest明示3本のheadlessテストを安定順・fail-fastで実行し、`npm run release:source`で構文、docs guard、全headlessを一括実行する。回帰1500、soak、browser、build、asset検査は変更種別に応じる別ゲートのまま。
- **独立QA**: 初版でmanifest内symlinkからrepository外JSを実行できるP2を発見。discovery root、明示include、実行直前を`realpathSync`で再検証する修正`edb92ec`後、同じ攻撃条件をexit 2で拒否し、外部JS未実行、15本PASSを確認した。
- **反映**: 包括委任の初回通常タスクとして`game-main`へ統合（`083b153`）。`npm run release:source`がPASSしたため、以後のsource品質ゲートとして使用する。

## 2026-08-14 — PUB-01: Store Candidate完成までkantoku-labを再ロック

- **ユーザー決定**: Steamストア撮影候補が架空表示で完成するまで、`kantoku-lab.pages.dev`をBasic認証で再ロックする。実名を使うバランス検証はローカルへ寄せる。
- **実装**: `lab/_worker.js`をfail-closedへ戻した（`7739dad`）。Cloudflare Pages暗号化secretの`LAB_PASS`が無ければ503、無認証・誤認証は401＋`WWW-Authenticate`＋`Cache-Control: no-store`、一致時だけ`env.ASSETS.fetch()`へ通す。秘密値はrepo・CLI引数・ログへ出さない。
- **品質証拠**: 専用6件＋独立異常系14件、既存11スイート581件、構文、docs guardがPASS。cleanな`integ/lab`からbuildし、`dist-lab/_worker.js`のバイト一致と未追跡draft非混入を確認後、正規`npm run deploy:lab`で本番反映。production aliasのroot／JS／開発ビューアが401であることを確認した。
- **既知の代償**: iOSホーム画面でBasic資格情報を忘れる既知UXは許容する。Basicは一時措置であり、Store Candidate後の公開時は架空表示・FN-02・権利台帳をゲートにして解除を再判断する。

## 2026-08-14 — Steam正式日程と4枠の半自律開発体制を採用

- **正式日程（ユーザー決定）**: **2026-09-30 Steam Coming Soon公開・ウィッシュリスト開始／2026-11-13 feature freeze／2026-11-30 製品build審査／2026-12-10 Windows版発売**。12/17は延期ではなく緊急予備日。旧「9月末Web v1.0、Steamは発売後」の計画を上書きする。
- **製品境界**: 初回はWindowsのみ。既存WebゲームをElectronの薄いラッパーで製品化し、全資産ローカル・オフライン進行・ファイル保存と復旧・Steam版の連続プレイをRelease Mustとする。Web版の1日1試合運用は別設定で維持する。
- **品質と期限の両立**: 9/6にStore Capture Candidateを凍結し、9/14をストア審査提出の内部締切とする。9/14以後はストア審査指摘とP0/P1だけ、11/13以後はrelease blockerだけを変更する。新機能数ではなく「初回30分の黄金導線」「黄金5画面」「2シーズン継続」「保存・オフライン・Windows安定性」を品質バーにする。
- **調整弁**: BGM、ポスト直撃、抗議、ベンチ、深い面談、ライブAI、Steam実績、Deck Verified、Mac/Linuxは発売後。引退/regen、補強放出、追加コーチ等は11/6までにsoak合格したものだけ採用する。
- **4枠の半自律体制**: Orchestrator/Release Integrator 1、Game/Platform writer 1、Experience/Art writer 1、独立QA 1。Reviewerはwriterの空き枠に交代で入る。書込エージェントは常時最大2、1タスク=1 worktree=1 branch=1 owner、共有ファイルは単独リースとする。
- **自走の境界**: タスク分解→実装→ローカル検証→独立QA/Review→最大3回の修正までは自動で進める。`game-main`統合、baseline更新、build/push/deploy、権利・AI申告、Steam Direct支払、審査提出・公開・set-liveは必ず人間ゲートを通す。
- **公開状態の緊急課題（PUB-01）**: 7/24のBasic認証撤去後、`kantoku-lab.pages.dev` は実名表示を含む状態でURLを知れば到達できる一方、旧正本は合い言葉ゲート維持と記載していた。再ロック、架空表示既定化、現状容認のいずれかを人間が即決する。推奨はStore Candidate完成まで再ロックし、実名バランス検証をローカルへ寄せる。

## 2026-08-14 — MG-06 コーチングチームMVPは「固定品質の助言・効果なし」で採用

- **ユーザー承認**: 分析コーチ＋フィジカルコーチのMVPを `game-main` へ統合し、kantoku-labへ反映する。
- **分析コーチ**: 既存の試合中コーチカードを人格化。確定済みシーンの `result/ofsPoint/dfsPoint` を**読み取るだけ**で、投入値でも5以上不利かつ実際に競り負けた自軍選手を報告する。該当なしは既存の「相手キープレイヤー／厳しいマーク」を分析コーチ名義で返す。
- **フィジカルコーチ**: 試合間に永続化されている `injuryOut` だけを読み、回復日を提案する。試合内 `fatigue` は週を跨がないため、存在しない疲労データを捏造しない。
- **効果を付けない理由**: 分析力による助言精度／回数はMG-15のサブスク案と競合するため保留。MVPは全員に標準スタッフ2名を提供し、能力値・buff・rng・デュエル判定／カウントへは配線しない。
- **セーブ互換**: MG-06前のv4では `coaches.physical=0` が未使用の固定値で、雇用UIも存在しなかった。プレイヤー選択を上書きしないため、ロード時に標準スタッフのレベル1へ補完して保存する。
- **検証**: MG-06専用10/10、リーグセーブ231/231、回帰1500緑、日英＋FN-00表示名層を確認。実装コミット `64ba656`。

## 2026-07-27 — システム選択を「8区分 → 型」の2段へ／キャプテンを監督が選べるように

- **決定（ユーザー指示）**: ①システムは数が多すぎるので **区分でまとめ、区分をタップ→次の画面で型を選ぶ** 2段構えにする。表示名は「区分名＋A/B/C…」（例: 4-4-2A / 4-4-2B）。②**監督がキャプテンを指名できる**ようにする（自動編成で自動的に決まってもよい）。
- **区分は6→8に増やした（ユーザー承認）**: 指定の6区分（4-4-2 / 4-3-3 / 3-5-2 / 3-6-1 / 5-4-1 / 5-3-2）に現行22システムを振り分けると **7つがどこにも入らない**（4-2-3-1系4つ・3-4-3系3つ）。**4-2-3-1 は22チーム、3-4-3 は5チームが既定布陣に採用中**で、これは「W杯2026初戦の実布陣」という実データ（[[football-sim-default-system-source]]）。落とすとデータの正確さを失うため、**「4-2-3-1」「3-4-3」を区分として追加し8区分**とした。
- **データは1行も動かさない（重要）**: `system_data` の**並び順と name は不変**のまま、`SYSTEM_GROUPS`（区分→member index）＋ `systemLabel()` という**表示層だけ**を足した。理由＝ **name は `TEAM_DATA.default_system` の引き当てキー／配列 index はリーグセーブ `_state.lineups[].systemIdx` のキー**。並べ替え・改名すると保存済みの布陣が静かに別フォーメーションに化ける（戦術IDを改名しないのと同じ理由）。
- **キャプテンは「読む瞬間に導出」**: `team1State.captain`（players index・-1=おまかせ）を保持し、**参照する瞬間に「スタメンに居るか」を検証**して、居なければ自動選出へ落とす（`effectiveCaptainIdx`）。ドラッグ入替・自動編成・交代・欠場…と布陣が変わる入口ごとに同期コードを足す設計にしない（[[qa-reactive-detector-antipattern]] と同型の再発防止）。自動選出の式は `autoCaptainIdx()` 1本に集約し、**表示（設定画面の「おまかせ」）と効果（mental.js のキャプテンシー）が割れない**ようにした。
- **落とし穴（実測で発見）**: `match.js` の `_buildSide` が override から state を組み直しており、**createMatch 経路（監督モード＝リーグの本線）でキャプテン指名が静かに落ちていた**。設定画面の表示だけ見て合格にすると気づけない＝**実際にキックオフして live チームの値を読むまでが検証**。
- **効果は既存のキャプテンシー（PS-02）に相乗り**＝失点時にチーム士気を+0.45（試合2回まで）。新しい判定式・新しい乱数は作っていない。回帰 check 1500 緑・`league-save-v4-test` 198/198 PASS。

## 2026-07-03 — 体力消費（疲労）＝時間経過による能力逓減を実装（DEV_NOTES①）

- **決定**: 全出場選手が**時間の経過**で能力低下（ユーザー指示・デュエル参加数ベースではない）。`低下率 = 0.30×(100-スタミナ)/100×(出場経過)²`・floor0.80（mental.js 4b・`fatigueParamFactor`＝getActionParam係数seam相乗り）。
- **数値の根拠**: ユーザー当初案「スタミナ50→90分で-50」は、実サッカー（終盤の高強度ラン低下20-40%だが技術実効値の低下は数%〜15%）とデュエル式k=2.7の増幅（-65%相対→フレッシュ側95%勝率＝ゲーム崩壊）に照らし過大→**スタミナ100=0%／50=-15%（相対）に縮小**しユーザー合意。2乗カーブで前半温存・60分以降加速＝実際の交代適期と一致。
- **交代の実効果が発生**: 経過は**選手ごとの出場チャンス数**＝途中出場はフレッシュ（70分投入なら90分時点-0.7%）。GKは対象外（走行負荷小＋内部スタミナ50固定の artifact 回避）。
- **KPI**: 平均合計得点 3.17→3.05（終盤の決定力が落ち実W杯目安2.7へ近づく）。日本vsブラジルの勝敗分布がスタミナ構成差で許容差超過＝意図的変更・baseline再採取。
- 検証: 係数テーブル理論値一致・seed再現6/6・総合力が終盤に沈む実測（日本-6.5%/蘭-2.9% at 90分）。

## 2026-07-03 — デュエル解決式のコントラスト指数 k=2.0→2.7（絶対ルールの初の意図的変更）

- **決定**: デュエル勝率を `o²/(o²+d²)` から **`o^k/(o^k+d^k)・k=2.7`**（`DUEL_EXPONENT` 定数・simulate.js `duelWinProb()`）へ。**ユーザー明示判断**（2026-07-03・デバッグ数値バンドで「90 vs 53.7 が3.8回に1回負けるのは多い→5回に1回にしたい」）。
- **効果**: 90 vs 53.7 の番狂わせ 26.3%→**19.9%**（実測一致）。互角は50%のまま・僅差はほぼ不変・大差ほど順当に（90vs30: 10%→4.9%）。
- **適用範囲**: 全デュエル系解決9箇所（通常デュエル2・競り合い2＝セットプレー/クロス・シュートvsGK4・CK1）を `duelWinProb()` に共通化。**PK専用解決とチャンス配分式 `t1/(t1+t2)` は対象外**。rng消費順不変＝seed再現維持（6/6実証）。
- **KPI変化（N=1500・k=2.0比）**: 平均合計得点 2.96→**3.17**・シュート 8.2→8.5・決定率 36→37%・強豪の勝率上昇（例: 日本vsチュニジア 勝62→68%・スペインvsモロッコ 勝75%）・**ブローアウト増**（片チーム≥5点 4.5〜9.3%・最大11点）＝懸案「7-1級再現」（2026-06-30 案B相当）にも前進。平均得点が実W杯目安2.7よりやや高くなった点は今後のチューニング課題（PS-07/エンジン精度トラック）。
- **運用**: baseline更新はユーザー承認後。「デュエルカウントロジックには触れない」ルールは**カウント（選手選抜・回数系）**に関するもので不変。解決式の指数は今後 `DUEL_EXPONENT` でチューニング可。

## 2026-08-06 — デプロイ調停ルールを制定（kantoku-lab の相互上書きが3往復）

- **事象**: G↔D の並行運用で、kantoku-lab へのデプロイが**相手の成果を丸ごと消す**事故が3往復（8/05〜8/06）。
- **真因（3点・どれも「注意」では防げない）**: ①**本番エイリアスは1本＝デプロイは置き換え**（gitは衝突しないので既存のハンク単位ルールでは防げない） ②**`dist-lab/` は gitignore＝成果物はローカルのみ**＝「誰かのローカルの状態」が本番になる／相手のコードは**マージして再ビルドしない限り入らない** ③**`--branch` 省略時の出力先が「今いる git ブランチ名」で暗黙に決まる**＝**同じコマンドが状況次第で本番を壊す**。
- **決定（`PARALLEL_SESSIONS.md`「デプロイ調停ルール」D-1〜D-8）**: ①**本番に出せるのは恒久統合ブランチ `integ/lab` からのみ**（毎回 integ/xxx を作らない） ②**`--branch` は毎回明示・省略禁止**（本番=`--branch=main`／確認=`--branch=<自分のブランチ>`＝Preview） ③**日常はPreviewで済ませ本番は節目だけ**＝衝突の機会自体を減らす ④**プリフライト必須**＝`git merge-base --is-ancestor <直前Productionのsha> HEAD` が false なら**出した瞬間に消える**ので中止 ⑤**統合後は必ず再ビルド**（`build.js` の `LAB_ONLY_JS` 競合は両側採用が正） ⑥**事後確認は自分＋相手の機能の両方**（★自分の機能だけ見て「出た」と判断したのが3往復の直接原因） ⑦本番デプロイは開始/完了をユーザーへ宣言＝人間が実質のロック。
- **反映**: `/deploy` スキルを改訂（**危険だった `--branch` 省略コマンドを削除**し、Preview経路7a／本番経路7b＝プリフライト込み／事後確認7c に分割。冒頭に「どこへ出すのか最初に確定する」節を追加）。
- **教訓**: **暗黙のデフォルト（--branch省略）は、並行運用では事故そのもの**。「同じコマンドが文脈次第で違う所に出る」設計を見つけたら、**明示を強制する**のが唯一の対策。

## 2026-07-30 — 架空化(FN-01)を最終盤へ後回し／βは合い言葉ゲート維持（実在データでバランス調整を最後まで）

- **決定（ユーザー方針）**: **試合パートのバランス調整を最後まで続ける。実在選手・実在チームでのテストが最適**なので、**架空化(FN-01/02)は最終盤（v1.0 一般公開の直前）へ後回し**。★B（β前必須）→ **★C（9月中旬）** へ変更。
- **根拠**: 回帰ハーネスの baseline も実在チーム基準。実在選手の能力値は「この結果は妥当か」を人間が判断できる唯一の物差しで、架空データに置き換えると**バランスの当たり判定を失う**。バランスが肝の試合パートを最後まで詰めるなら、実在データを最後まで残すのが正しい。
- **⚠️ 必然の帰結＝βの定義変更**: 架空化前に実名で一般公開すると権利リスク。よって **β（8月下旬）は「一般公開」ではなく「合い言葉ゲートを維持した拡大クローズドテスト」**（`_worker.js` Basic認証を残す）。D7/D30 の測定は Discord 経由の招待で可能＝検証目的は損なわない。**一般公開は v1.0（9月末・架空化済み）から**。
- **リスク低減＝表示名インダイレクション方式で確定（BACKLOG FN-01 の案(b)を採用）**: `long_name` を**内部ID（セーブキー）として据え置き**、表示のみ差し替えるマッピング層を**先に作る**（8月中に低コストで導入）。
  - 理由: v4セーブの `squads[クラブ][選手キー]`（成長delta／怪我・停止の残り／出場・得点）は選手名をキーにしており、名前を実データごと変えると**エラーも出ずに保存データが全て迷子**になる（静かなデータ消失）。インダイレクションなら**最終盤の差し替えはフラグ1つ**＝セーブ互換もβユーザーの継続も守れる。
  - 副産物: 開発中は「実名表示／架空表示」をトグルで切替でき、FN-02（実名残留の機械チェック）を**早期から回せる**＝最終盤の一発勝負を避けられる。
- **デッドライン**: FN-01/02 は **9月中旬（〜9/20）** に完了必須。9月下旬は磨き込み＋調整弁の判定に充てるため、ここを越えると v1.0 一般公開に載らない。
- **バランス調整トラックの明示**: 試合バランス（デュエル指数・KPI・PS/MG係数・ポスト直撃等）は**9月末まで継続**する常設トラックとして扱う（実在データで検証・baseline更新はユーザー承認）。

## 2026-07-14 — 画面向き＝横長(landscape)へ正式転換（2026-06-30 の凍結を解除・上書き）

- **決定（ユーザー承認・週次チェック時）**: リーグ（kantoku-lab）系UIを **横長(landscape)3ゾーンレイアウト＝キャプ翼近代版に正式転換**。2026-06-30 の「縦長維持・横化は9月まで凍結」を**破棄**。以降の新規UI・残りの画面はすべて**横長で作る**（縦長への差し戻しはしない）。
- **経緯**: 7/11〜7/12 に lab で実装（`dc49c91` 監督試合画面3段レイアウト／`02f9a53` リーグハブ横長化／`094f834` 監督試合を横枠内に／`8f2ff12` 采配設定を横3ゾーン／`d3c8ffc` プレー画面全面最大化）。当初は探索だったが、キャプ翼近代版の横長が体験として優れると判断しユーザーが正式採用。
- **"安い保険"が効いた**: 2026-06-30 で内包した「相対単位＋中央/左右3ゾーン分離構造」のおかげで、横化が想定より安価に実現。保険を早期に現金化した形。
- **影響**: ①**横化はP10でなく今（MVP正系）**＝Steam資産の16:9問題は将来むしろ有利に働く（縦の黒帯懸念が消える）。②残りのα準備画面（ホーム/試合前/レポート/順位/オンボ等）は横長前提で設計。③既存の公開サイト（football-sim.com・縦）は凍結中で不変＝リーグ別URLのみ横長。
- **凍結中の懸案（DECISIONS 2026-06-30 の根拠②③は今も有効）**: Steam Deck 横16:10・近接ジャンルの横UIは、この転換で**むしろ整合**（縦→横の作り直しコストが先に消えた）。
- **要フォロー**: BACKLOG「UI/レイアウト作法」の"縦長=正系"記述を横長基準へ更新（次のUI作業時）。

## 2026-06-30 — 画面向き＝縦長維持＋"安い保険"（⚠️2026-07-14 に上書き・下記は履歴）

- **決定**: 試合パートのリデザインに着手するにあたり、画面向きを **縦長(portrait)＝MVPの正系で維持**。今は横長に切り替えない。横向き最終決定は **9月の合否判定(D7/D30)まで凍結**。 ⚠️**2026-07-14 に横長へ正式転換したため無効（上記参照）**。
- **根拠（gtm-strategist 調査・一次情報）**: ①Steam はゲーム自体は縦を許容するが**ストア資産(スクショ/カプセル)は16:9強制** → 縦UIそのままは黒帯(pillarbox)で「モバイル移植」に見えウィッシュリスト転換が鈍るリスク（[Steam公式アセット仕様](https://partner.steamgames.com/doc/store/assets/standard)）。②縦のままSteam成功例は **Vampire Survivors＝アクション主役型**で、我々に近い**判断/物語/管理シム（Reigns・Football Manager・VN）は全てPCで横UIに作り直し**。③**Steam Deck は横16:10**で縦UIはVerified困難・Playable止まり。④だが**MVP検証(D7リテンション)にSteamの見栄えは無関係**＝Steamは「Webで面白さ証明後」の販路拡張(ROADMAP P10・12月以降)。よって縦優先は商業性と矛盾せず**順序の問題**。
- **VISION適合**: コアループ(毎日8〜15分・漫画コマを"読む")はポートレート・ネイティブ。縦で読むのが自然＝USP。
- **"安い保険"（今のリデザインに内包・BACKLOG「UI/レイアウト作法」に明文化）**: ①**固定px禁止→相対単位/レスポンシブ** ②**「中央＋左右サイド」に分離できるDOM/CSS構造**。これで将来 ①framed portrait（縦中央＋左右を装飾アートで16:9化）→ ②横3ゾーン化（左右を順位/SNS/選手情報の生きたパネルへ）へ**安く段階移行**できる。横化は機能でなく"作り方の作法"なのでP3〜P6に内包。
- **試合パートのアートディレクション（ユーザー選択）**: **B(TV中継/Broadcast)を核＝スコアバグ/試合時計/ポゼッションバー**＋**実況テキスト枠だけ A(週刊サッカー漫画)の黒帯ネーム(キャプション)**を採用するハイブリッド。比較プロトタイプ `tools/proto/design-directions.html`（A漫画/B中継/C戦術ビューの3案・ダーク・モバイル想定）。担当 = ui-designer(Canvasの外=HUD/フレーム/実況/操作) ＋ renderer-dev(Canvasの中=カットイン絵)。
- **体制変更**: ui-designer（ビジュアル/HTML/CSS・Canvasの外）と gtm-strategist（GTM調査・計画専任・対外アクション禁止）を ~/.claude/agents に新設。wc-ratings スキル＋日次リマインダーは廃止（W杯後レーティング保守は data-steward が随時）。

## 2026-06-30 — リアル要素: コーナーキック＆ペナルティ（v2.5.0）

- **目的**: ユーザー要望「CK・ペナルティエリア内ファール→PK のリアル要素」。T-15(采配ドラマ核=ゲーム的)より先に realism を優先。DEV_NOTES ③ の実装。
- **方式＝result-hook（デュエル式・カウント不可侵）**: 既存の result にフックして「シーン＋専用解決」を加算。
  - **PK**: FW(ボックス)エリアのファール（既存・フェアプレー idx28 判定）のうち一部を PK 化（中央 FW_M=0.20/サイド=0.08）。専用解決＝キッカー(idx11最良) vs GK(idx23)・成功率 `0.75+(shoot-gk)*0.004`[0.55,0.92]（既存 `wcsimPenaltyShootout` 流用）。foul→PK は主審ビートを重複させず接触のみ。
  - **CK**: 被セーブ/ブロックから一定確率(0.30/0.22)でコーナー1本（テンポ優先で最大1）。競り合いは既存デュエル式・守備密集で守備有利(×1.5)＋合わせ低質(×0.6)で決定率を現実域に。
- **発生率（実測・現実域に調整）**: PK 0.28/試合(成功率75%)・CK 0.96/試合(決定率5.1%)。
- **KPI（意図的バランス変更→再ベースライン）**: 平均合計得点 2.81→**2.95**・シュート 7.8→8.2。実W杯2.7よりやや上振れだが realism 優先でユーザー承認。
- **演出（表示層・ユーザー反復で確定）**: PK=①倒される→②**PENALTY!（画面いっぱい）**→③シュート絵(shot_<kit>)で蹴る→④結果（GKダイブ中間ビートは不自然で削除＝3ビート）。CK=ヘディング競り合い（header_clash/rise の赤/緑空中戦を赤→攻撃色/緑→守備色にリカラー・成功=ヘディング！/失敗=競り負け）。確認用 `tools/proto/ckpk-test.html` 新設（PK/CKは稀なため常時確認）。
- **三重チェック**: reviewer(デュエル式/カウント不可侵PASS)／qa-regression(events再現1800/1800・KPI妥当)／**Codex がエンジン045c7d0=バグ無し・PK分割4a038e4で docs 不整合[P2]を指摘→再ビルドで解消**。
- 隔離ブランチ `feat/ck-pk`→人間承認→main FFマージ→build→push。デュエル式・カウント無改変。

## 2026-06-30 — P2 監督ビューア（T-09/T-11/T-12）デプロイ（v2.4.0）

- **目的**: VISION「監督として試合を読む」コア。漫画コマ送りビューア＋采配介入を本番化。
- **設計の肝＝既存シーン送りシーケンサの再利用**: `simulate.js` の `nextChance` UI（`_shootSplit`/`renderShootStep`/`renderSceneArt`/`renderSceneField`/`showGoalCutscene`/HT）は完成度が高い。作り直さず、**データ源だけ「startGame の事前一括演算」→「createMatch の遅延実行」に差し替え**。新 `js/manager-match.js`（監督モード）が `createMatch` を駆動し、`chanceResults` をチャンス単位で遅延 push。本番の通常試合フローは `_managerMode` ガードで完全分離＝**回帰ゼロ・挙動ビット同一**。
- **なぜ createMatch（遅延実行）か**: 従来の後半交代 `_recalcSecondHalf` は「残りを再シミュレート」＝RNG再抽選・チーム再構築で fatigue 消失・非決定的。createMatch は未計算チャンスを1つずつ計算するため、采配で lineup/tactics を差し替えるだけで次チャンス以降に反映＝再抽選なし・走行状態保持・seed再現可。
- **テンポ（ユーザー判断）**: 「自動再生＋采配点で停止」。HT/ゴールで自動停止、任意で一時停止。1ビートの待ちを **1×=2400ms**（最長アニメ＝ワンツー2200ms を切らない）/2×=1300/3×=700。32チャンスで全コマ手送りは長すぎるため自動再生主体に。
- **采配UI（ユーザーFBで拡張）**: 当初は交代/戦術の簡易ピッカーだったが、ユーザー指摘で**プリマッチと同じ「設定画面」を監督モードから再利用**へ変更（システム/戦術/キープレイヤー/要注意/出場選手の入替/交代を全部）。設定画面の各セレクタは全て team1State を編集するため、采配を開く時 live チーム→team1State 同期、閉じる時 team1State→live チーム適用。HT はさらに**既存 `_showHalfTimeModal` を再利用**（デュエル状況＋コーチ助言を復活）、後半キックオフは `closeHalfTimeModal` の `_managerMode` 分岐で再シミュレートせず再開。コーチカード(`_maybeInsertCoachCard`)・交代ログ(`_insertSubLog`)も両モードで出すよう復活。
- **介入は home（自チーム）のみ**＝相手AI(team2)采配は Sprint3。「喝/モチベ補正」は morale 層の別タスク（未実装）。
- **三重チェック（4ラウンド）**: reviewer（全ガードレール・通常モード非破壊PASS）／qa-regression（回帰緑2.77・構文・ブラウザ動線・エラー0）／**Codex 独立レビューが実バグを計4件発見**（①交代の過去スコア誤帰属＝過去scene凍結で解消 ②buildキャッシュバスト正規表現がハイフン名 `manager-match.js` 不一致 ③ツールバー采配後の停止種別引き継ぎ ④HT通過後に通常試合の all-btn が無効化残り）＝**別系モデルの価値を再実証**。全て修正・実機検証。
- 隔離ブランチ `feat/p2-match-viewer`→人間承認→main FFマージ→build→push。エンジン（デュエル式/simulateChance/カウント）無改変。

## 2026-06-30 — エンジン精度改善: 通常チャンス数 16→32（v2.3.0・デプロイ済 `e570101`）

- **目的**: 得点/シュート数を実W杯水準へ底上げ（KPI=得点・シュート）。実例の物差し＝2026 ドイツ 7-1 キュラソー（合計8得点・シュート34本）。
- **現状診断（実測）**: 旧16チャンスは 合計得点1.39・**合計シュート3.9本**（実W杯~24）・**決定率35%**（実~11%）＝「少ない決定機を高確率で決める」非現実状態。
- **ユーザー判断（対話で確定）**: ①リアル化方針=**全試合の平均を底上げ**（ミスマッチ拡大でなく）②シュート目標=**中程度→最終的に7〜8本に緩和**（チャンス数だけで10-14本を出すには~48コマ＝試合が長すぎるため）③主軸=**チャンス数を増やす**（到達率・決定率ロジックには触れない最シンプル路線）。
- **採用**: `MATCH_CHANCES=32`/`HALF_CHANCES=16` を定数化し、`calcTime` の時間割りをパラメトリック化。全チャンスループ・前後半分割(旧8)・playMatch(`match.js BASE_CHANCES`)・回帰ハーネスを定数追従。**デュエル解決式 `ofs²/(ofs²+dfs²)` とカウントロジックは不変**＝各シーンの確率挙動は無歪み（回帰のシーン結果率が一致: 成功44.1%等）。`fatigue`/`chance_counter` は確率計算に未使用と確認。
- **結果（代表6カード平均・回帰再採取済）**: 合計得点 1.39→**2.78**(目標2.7)／合計シュート 3.9→**7.8**(目標7-8)／枠内 2.8→5.6／片チーム≥5点 0.2%→**3.5%**／最大得点 5→**8**。6-1/7-1 が裾事象として出現（ユーザーが100試合で6-1複数回を確認・承認）。
- **三重チェック**: reviewer（規約・デュエル不可侵=PASS）／**Codex 独立レビューが `match.js BASE_CHANCES=16` の追従漏れを発見**（reviewer見逃し・修正済）／qa-regression（回帰PASS＋ブラウザ実起動: 32チャンス/ハーフタイム index16/時間1-90/エラー0）。
- **「リアルさの物差し」**: 回帰ハーネスにシュート/枠内/決定率/得点分布の裾（片チーム≥5点率・最大得点）を追加し、実W杯目安を併記。意図的バランス変更につき baseline 再採取（N=2000）。
- **残課題（7-1の本格再現）**: ドイツ×キュラソーは平均2.16-0.62・ちょうど7-1は0.10%止まり。原因＝①キュラソーのデータが強すぎる(TotalParam20071≈USA) ②**チャンス配分式 `t1/(t1+t2)` が強弱差を圧縮**（独52%止まり・実7-1は独76%シェア）。本格再現には案A(データ弱体化)/案B(配分式にコントラスト指数)/案C(現状=リアルな平均＋7-1は稀な裾)の判断が必要＝**未決・ユーザー相談待ち**。主目標(平均底上げ)は達成。
- 本変更はゲーム化トラックとは別の「エンジン精度」トラックとして隔離ブランチ `feat/engine-accuracy` で実施→人間承認→main FFマージ→build→push。

## 2026-06-19 — 試合ビジュアルは方式C（ハイブリッド）＋ AI生成アセット

- ユーザー提示の参照クオリティ（Instagram the.goal.club 級の作り込みピクセルアート）は、**実行時 procedural 描画では到達不能**と判断。あれは1枚ずつのプリレンダ作品（手描き or AI生成＋補正）。
- 採用 = **方式C ハイブリッド**: 通常プレーは軽量な動的描画（procedural、[tools/proto/manga-prototype.html](tools/proto/manga-prototype.html) v3 系）、**ゴール/決定機は参照級のプリレンダ・カットイン**。
- アート調達 = **AI画像生成（ChatGPT等）**。Claude Code は画像生成ツール非搭載のため「絵」は作らず、**「使う仕組み」**（生成ガイド/プロンプト・マニフェスト・名場面検出・HUD重ね・統合）を担当。
- 制約: 全チーム・任意選手を扱うため、カットシーンは**汎用**（選手別の似顔は出さない）。チーム同定は HUD（名前・カラー）で行う。主要モーメントは**キット色違いを数種**用意し近い色を選ぶ。
- IP: 実在選手の似顔・実クラブ徽章・スポンサーロゴは使わない（汎用顔・架空のソリッドキット）。テキスト固有名詞（[CLAUDE.md](CLAUDE.md) 禁止語）より踏み込んだリスクのため厳守。
- カットシーンの粗さ標準 = **横140px / 28色**（ユーザー選択A）。`pixelate.js`/`gen.js` の既定に反映。実証: `goal_bicycle_red_01`（参照級画像→粗ドット→HUD合成までエンド・ツー・エンドで動作確認）。パイプライン: [tools/art/](tools/art) gen.js + pixelate.js + prompts.js。

## 2026-06-19 — スプリント順序を変更（漫画レンダラを先に）

- パイロット（Sprint 1）を「怪我/退場」→「漫画マッチアップ・レンダラ」に変更（ユーザー判断）。理由: ビジュアルが最重要要望であり目玉。レンダラはプレゼン層のみでエンジン無改変＝回帰ハーネスは自明に緑で安全に進められる。怪我/退場は Sprint 2 へ。
- 進め方: 画風はピクセルアート/漫画調で主観差が出やすいため、まず単体 Canvas プロトタイプで画風合意 → その後ライブ試合描画へ統合。外部画像アセットは持たず、コードでピクセル figure を手続き描画（パラメトリック: ポーズ×着色レイヤー）。

## 2026-06-19 — Sprint 0 着手・方針確定

- **ゲーム化の核 = 監督体験**。既存資産（W杯/大会sim・選手DB・戦術・交代）を最大活用し、試合は「観戦＋ベンチ指示」、その上にキャリア/大会の長期構造を載せる。
- **試合ビジュアル = 漫画的マッチアップ × ドット絵**。エンジンが元々1対1のマッチアップを吐くため、22人連続アニメより各シーンを漫画コマとして描く方が直結・低リスク。選手はパラメトリック・スプライト（ポーズ×着色レイヤー）で個別作画を回避。
- **自走の進め方 = 設計合意 → 1機能で試走 → 本格化**。パイロットは怪我/退場（最小ブラスト半径で自走ループ全工程を通せる）。

- **回帰ハーネスを最優先で実装**（Sprint 0 の最初）。理由: 本番稼働中にエージェントの自動変更を通すには、得点率・勝敗分布・イベント発生率の静かなドリフトを機械検出する安全網が前提。これが無いと自律実装を本番に流せない。
  - 旧 `sim_test.js` / `calibrate_large.js` は index.html から `<script>` を行番号抽出する方式で、2026/06/02 の js/ 分割以降は動かない。後継として `tools/lib/load-engine.js`（分割 js/ を連結し vm context で実行）を新設。
  - `simulateSilent()` と同じ手順で `simulateChance()` を回し、生シーンから結果種別の発生率も集計（将来のカード率・怪我率の土台）。
  - 統計的判定（許容差）。完全再現はシード可能RNG導入後（BACKLOG）。
  - 基準スナップショット: 全体平均合計得点 ≈ 1.39、シーン結果率 成功44%/失敗36%/カウンター5.2%/セーブ4.2%/ゴール4.0%/ファール3.2%/枠外2.5%/ブロック0.8%。

- **計画ドキュメントは repo root に置く**（`docs/` は build 成果物で公開・上書きされるため不可）。`build.js` は `js/`・`css/`・`img/`・`index.html`・`CNAME` のみ docs/ へ出力するので、root の .md と `tools/` は公開も clobber もされない。
