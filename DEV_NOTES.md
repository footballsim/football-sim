# 開発検討メモ — 試合パート改善案

最終更新: 2026-07-28（実装状況を全案で棚卸し）／ ステータス: **8案中5案が実装済み・残り2.5案**

試合（マッチ）体験を深める改善アイデアの保管庫。ユーザー提案を起点に、各案へ「実装フック・既存資産・ロードマップ上の位置・ガードレール」を付記。
関連: [VISION.md](VISION.md) / [BACKLOG.md](BACKLOG.md) / [GAME_PLAN.md](GAME_PLAN.md) / [AGENTS.md](AGENTS.md)。
**共通の鉄則**: デュエル解決式 `ofs²/(ofs²+dfs²)` とカウント・ロジックは触らない（既知バグ）。パラメータ補正・イベント発火・表示層で実現する。戦術は実在4種のみ。

## 現況サマリー（2026-07-28 コード確認）

| 案 | 状態 | 実体 |
|----|------|------|
| ① 体力消費 | ✅ 実装済み | `js/mental.js` `fatigueParamFactor`/`fatigueOnChanceEnd` |
| ② モチベーション | ✅ 実装済み | `js/mental.js`（メンタル層 PS-01〜08） |
| ③ CK・PK | ✅ 実装済み | `js/simulate.js` CK=3231〜 / PK=3068〜 |
| ④ 怪我・退場 | ✅ 実装済み | `js/discipline.js`＋`events.js` CARD/INJURY |
| ⑤ ホームアドバンテージ | ❌ 未実装 | `iAmHome` は表示・記録のみでparam効果なし |
| ⑥ 観客ノイズ | 🔶 演出側で進行中 | 語彙は未追加。カットシーンFX層で着手 |
| ⑦ 戦術コスト | 🔶 半分 | 支配率トレードオフのみ。体力消耗リンク未 |
| ⑧ 前半交代 | ✅ 実装済み | `_updateSubBtn` が前半から解放 |

## テーマ別の見取り図
- **A. 試合に「結果（消耗・心理）」を持たせる層** … ①体力消費 ②モチベ ⑦戦術コスト。**チャンス数32化で試合が長くなった今、終盤の失速・勢いが効きやすい**。相互連動する一体の層として設計すると良い。
- **B. 試合イベントを増やす層** … ③CK/PK ④怪我/退場 ⑥観客ノイズ。`js/events.js` のイベント語彙に追加し、演出(cutscene)・実況・統計が購読。
- **C. 文脈補正の層** … ⑤ホームアドバンテージ。
- **D. 監督操作の自由度** … ⑧前半交代。

## 個別案

### ① 体力消費によるパラメータ減少 — ✅ 実装済み
既存 `fatigue`（idx1スタミナ関連）を活用し、チャンス経過で蓄積→終盤にスピード/精度/守備系paramを逓減（**読み取り時の係数補正**）。
- ✅ **実装**: `js/mental.js` の `fatigueOnChanceEnd`（出場チャンス数を加算）＋ `fatigueParamFactor`（`getActionParam` の f へ乗算＝メンタル係数と同seam）。素のスタミナが低いほど落ちる曲線・GK対象外・`FATIGUE_ENABLED=false` のキルスイッチ付き。`MENTAL_ENABLED` とは独立。
- ロードマップ: VISION P5（コンディション）と連動。MATCH_CHANCES=32 で終盤失速が映える。
- ⚠️ デュエル式不変・param側で表現。
- 📌 **2026-07-03**: BACKLOG「監督キャリア トラック」MG-06 フィジカルコーチの助言（「◯選手のコンディションが悪い」）は本件のコンディションデータが**先行依存**。

### ② モチベーション概念 — ✅ 実装済み
VISION データモデルに `morale(-1..+1)` を定義済み。マッチアップ時に攻/守paramへ係数。
- ✅ **実装**: `js/mental.js`（メンタル層）。心理状態は `fatigue` リセットに相乗りして初期化、`getActionParam` の同seamで係数化。性格7種は PS-05 で拡張済み。
- ドライバ: 監督の「喝」（VISION 采配ドラマ）・試合展開（失点/得点/劣勢）・観客（⑥連動）。
- ⚠️ 同上（param補正で）。
- ✅ **2026-07-03 スケジュール化**: 性格/個性・特殊スキル・試合中イベントと一体の**メンタル層**として [BACKLOG.md](BACKLOG.md)「個性・メンタル・スキル トラック（PS-01〜08）」に計画化（設計方針は [GAME_PLAN.md](GAME_PLAN.md)）。⑥観客の「見える化」もこの層が購読先。

### ③ コーナーキック・PK イベント — ✅ 実装済み
現状はセットプレー/直接FKのみ。CK=クロス系の派生、PK=エリア内ファウル/重度反則からの専用解決（シュート精度 vs セービング＝既存PK戦ロジック `wcsimPenaltyShootout` を流用可）。
- ✅ **CK 実装**: `js/simulate.js:3231〜` の result-hook。直近シーンが「GK防いだ！／ブロック」なら確率でCK1本を生成（テンポ優先で最大1本）。競り合いは既存デュエル式流用・決定率は低め。演出は `cutscene.js` でヘディング競り合いの絵に統一。
- ✅ **PK 実装**: `js/simulate.js:3068〜`。CRエリアのファール＋`pkPending` から専用解決（デュエル式は使わず＝成功率 0.75 +(シュート精度-セービング)*0.004、55〜92%）。演出は「PK判定→キッカー→GKダイビング→結果」の4ビート。規律で退場/負傷の選手はキッカー候補から除外。
- `events.js` は `scenario === 'ペナルティキック' / 'コーナーキック'` を購読済み。

### ④ 怪我・退場イベント — ✅ 実装済み
**既に VISION/BACKLOG の Sprint2 / P7 に計画あり**。`events.js` は `card`/`injury` 語彙を**定義済み（未emit）**＝ここで発火実装。
- ✅ **実装**: `js/discipline.js`（Sprint 2/2b・約25KB）。`disciplineOnFoul` でイエロー/2枚目イエロー=レッド/一発レッドを段階化、`injuryParamFactor` で軽傷の能力減、`severity: 'minor'|'severe'` で続行可否。除外は選手フラグ基準（`_sentOff`/`_injured`/`_discExcluded`）。`DISCIPLINE_ENABLED=false` のキルスイッチ付き。
- ✅ `events.js:304〜` が `res.disciplineEvents` を CARD/INJURY イベントへ変換（`card`/`sentOff`/`severity` を保持）。
- ✅ 派生: DC-01「イエロー持ちの守備の不安」（244b9e9）、リーグ側の離脱者は布陣・ベンチから起用不可（9de3a40 / ba958e7）。
- ⚠️ result を「フック」する加算実装に限定（デュエル解決自体は不変）。

### ⑤ ホームアドバンテージ — ❌ 未実装（残タスク）
メキシコ戦で実装済みの開催地補正を一般化。Club/Match データに hostAdvantage を持たせ、開催地チームへ小幅補正。
- 現況: リーグの `iAmHome` は**表示と順位表の記録にしか使われておらず**、param への効果はゼロ（`js/league.js:3461` 等）。
- 実装余地: `_buildSide(data, override, isHome)`（`js/match.js:63`）が既に isHome を受け取っているので、ここが自然なseam。
- ⚠️ バランス変更＝回帰ハーネスのKPIで測って再ベースライン。

### ⑥ 観客ノイズイベント（演出のみ） — 🔶 演出側で進行中
パラメータ変更なしのテキスト演出（「地元の大声援でメキシコが奮起！」等）。`events.js` に `crowd`/`atmosphere` イベント型→cutscene/実況で差し込み。
- 現況: `events.js` に `crowd`/`atmosphere` 語彙は**まだ無い**。
- 🔶 **2026-07-28 着手**: カットシーンの共通FX層（`_csFx`）として、観客席のフラッシュ明滅・打字字幕・ナイター光芒などを実装中。**param不変の演出だけで雰囲気を出す**という本案の狙いと同一。イベント語彙の追加はFX層が固まってから（語彙→購読の順）。
- 低コスト・高雰囲気。VISION「AIが書く生きた世界」と好相性。②モチベの“見える化”にも（雰囲気だけならparam不変／連動なら②経由）。

### ⑦ 戦術効果の見直し — 🔶 半分実装
現状4戦術は主に攻撃係数。各戦術に**コスト/副作用**を付与（例: PRESS=体力消耗が早い＝①連動、COUNTER=被支配率上昇 等）。
- ✅ **支配率のトレードオフは実装済み**: `js/simulate.js:2738〜` で POSSESSION ×1.30 / PRESS ×0.90 / COUNTER ×0.85 / CATENACCIO ×0.70（＝守備的戦術ほどボールを持てない）。
- ❌ **体力消耗リンクは未**: `mental.js` は `tactics` を一切参照していない＝「PRESSは疲れが早い」が未表現。①が実装済みになった今、`fatigueOnChanceEnd` に戦術係数を掛けるだけで成立する（最小の残作業）。
- ⚠️ **実在4種（POSSESSION/PRESS/COUNTER/CATENACCIO）の範囲で**。新戦術・新フォーメーションは作らない。

### ⑧ 前半から選手交代可能に — ✅ 実装済み
現状の交代インフラ（`_subbedOff`/subsCount）を前半から開放。
- ✅ **実装**: `_updateSubBtn`（`js/simulate.js:3915〜`）が `currentChanceIdx >= 6` で交代ボタンを解放＝前半（0〜15）の途中から交代できる。5人・3回の枠管理は維持（ハーフタイムは回数を消費しない）。
- 📌 **相手AIだけは前半交代しない**（`js/manager-match.js:555`「現実的に稀」）＝意図した非対称。
- VISION P2 の介入点（交代）と直結＝監督の采配自由度UP。

## 実装着手時の進め方
- いずれも**三重チェック**（qa-regression=回帰/KPI × reviewer=規約 × Codex=独立バグ）を通す。
- バランスを意図的に変える案（①②⑤⑦）は**回帰ハーネスのKPIで影響を測り、再ベースライン**（精度改善トラックと同じ運用）。
- イベント追加（③④⑥）は `events.js` 語彙拡張 → 演出/実況/統計が購読、の順。
